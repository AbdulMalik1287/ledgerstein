# Kosh backend. The reconciliation core is deliberately stdlib-only -- no
# pandas, no rapidfuzz -- so the matcher stays portable and its throughput
# numbers are honest rather than borrowed from a C extension.
fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlalchemy==2.0.52
pydantic==2.13.5
python-multipart==0.0.20
anthropic==1.3.0

# Dev
pytest==9.1.1
httpx==0.28.1  # FastAPI TestClient
